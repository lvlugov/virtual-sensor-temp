# Copy to .env (or run `make env` to bootstrap from this template).
# Never commit .env — it's gitignored.

# Visual Crossing weather API key. Sign up at https://www.visualcrossing.com/
# Free tier: 1000 records/day. Required for
# external_temperature.fetch_bulk_to_disk / fetch_hourly_window.
VISUAL_CROSSING_API_KEY=
